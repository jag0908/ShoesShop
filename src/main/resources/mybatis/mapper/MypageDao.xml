<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.himedia.shop.dao.IMypageDao">

    <update id="updateMember" parameterType="com.himedia.shop.dto.MemberVO">
        update member set pwd=#{pwd}, name=#{name}, email=#{email}, phone=#{phone},
        zip_num=#{zip_num}, address1=#{address1}, address2=#{address2}, address3=#{address3}
        where userid=#{userid}
    </update>

    <insert id="insertCart">
        insert into cart(userid, pseq, quantity) values(#{userid}, #{pseq}, #{quantity})
    </insert>

    <select id="selectCart" resultType="com.himedia.shop.dto.CartVO">
        select * from cart_view where userid=#{userid}
    </select>

    <delete id="deleteCart">
        delete from cart where cseq=#{cseq}
    </delete>

    <select id="getCart" resultType="com.himedia.shop.dto.CartVO">
        select  * from cart_view where cseq=#{cseq}
    </select>

    <insert id="insertOrders">
        insert into orders(userid) values(#{userid})
    </insert>

    <select id="lookupMaxOseq" resultType="_int">
        select max(oseq) from orders where userid=#{userid}
    </select>

    <insert id="insertOrderDetail" parameterType="com.himedia.shop.dto.OrderVO">
        insert into order_detail(oseq, pseq, quantity, zip_num, address1, address2, address3)
        values (#{oseq}, #{pseq}, #{quantity}, #{zip_num}, #{address1}, #{address2}, #{address3})
    </insert>

    <select id="selectOrderByOseq" resultType="com.himedia.shop.dto.OrderVO">
        select * from order_view where oseq=#{oseq} order by odseq desc
    </select>

    <select id="getOseqIng" resultType="int">
        select distinct oseq from order_view where userid=#{userid} and (result='1' or result='2' or result='3') order by oseq desc
    </select>

    <select id="getOseqAll" resultType="int">
        select distinct oseq from order_view where userid=#{userid} order by oseq desc
    </select>

    <select id="lookupMaxCseq" resultType="_int">
        select max(cseq) from cart where userid=#{userid}
    </select>

    <delete id="deleteMember">
        delete from member where userid=#{userid}
    </delete>

</mapper>